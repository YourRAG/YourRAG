# ==============================================
# Database Configuration / 数据库配置
# ==============================================
POSTGRES_DB=rag_db
POSTGRES_USER=testuser
POSTGRES_PASSWORD=123456
# Embedding Vector Dimensions / 嵌入向量维度
EMBEDDING_VECTOR_DIMENSION=1024

# ==============================================
# GitHub OAuth Configuration / GitHub 登录配置
# ==============================================
# Create a GitHub OAuth App at: https://github.com/settings/developers
# 在 https://github.com/settings/developers 创建 GitHub OAuth 应用
# /auth/github/callback
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret

# Gitee OAuth Configuration / Gitee 登录配置
# ==============================================
# Create a Gitee OAuth App at: https://gitee.com/oauth/applications
# 在 https://gitee.com/oauth/applications 创建 Gitee OAuth 应用
# /auth/gitee/callback
GITEE_CLIENT_ID=your_gitee_client_id
GITEE_CLIENT_SECRET=your_gitee_client_secret

# Frontend URL for OAuth redirect (Where to redirect after login)
# 前端地址，用于 OAuth 登录后的回调跳转 (生产环境请修改为实际域名)
FRONTEND_URL=http://localhost:3000

# ==============================================
# JWT Configuration / JWT 令牌配置
# ==============================================
# RSA keys in PEM format with escaped newlines
# RSA 密钥对 (PEM 格式，换行符需转义)
# Optional: If left empty, keys will be automatically generated and stored in the database on first startup.
# 可选：如果留空，应用将在首次启动时自动生成密钥
# PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----"
# PUBLIC_KEY="-----BEGIN PUBLIC KEY-----\n...\n-----END PUBLIC KEY-----"

# ==============================================
# Redis Configuration / Redis 缓存配置
# ==============================================
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=123456
REDIS_DB=0
